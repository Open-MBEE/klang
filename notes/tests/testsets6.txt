Status: PASSING (as of 2026-01-20)
Category: Set Display and Universal Sets

History:
- Was: BUG - Displaying [Empty] for universal sets
- Fixed: 2026-01-19 - Set display for universal/empty sets
- Fixed: 2026-01-20 - Let-binding resolution for finite sets

Description:
Tests Set constraints with spacecraft fleets:
- LowCostSCFleet - all spacecraft must have cost < 1e6
- FeasibleLowCostSCFleet - all spacecraft must go to Earth

Current Output:
  FeasibleLowCostSCFleet(deployedSC:: Set(*))
  LowCostSCFleet(deployedSC:: Set(*))

The Set(*) indicates a universal set (all Spacecraft are members).
This is correct because the constraints use `sc isin deployedSC =>`,
which is trivially satisfied when deployedSC is universal.

Fixes Applied:
1. parseInlineSetExpression() - handles Z3 constant arrays (Set(*), Set())
2. extractLetBindings() - resolves a!N let-binding references
3. formatArrayValue() - formats Array types (used by sets internally)
