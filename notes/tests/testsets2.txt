Status: PASSING (as of 2026-01-20)
Category: Set Comprehension with Expression

History:
- Was: K2SMT ERROR
- Fixed: Set comprehension with computed expressions now works

Description:
This test uses set comprehensions with computed expressions:

  req s1 = Set{x + 1 | x : Int :- 10 <= x && x <= 15}

  req Set{x | x :- 11 <= x && x <= 14}
      subset
      Set{x + 1 | x : Int :- 10 <= x && x <= 15}

This requires generating computed sets and comparing with subset operations.
The implementation now handles expression-based element computation correctly.
